name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python test_dragons_and_princesses.py
    
    - name: Test with YAML input
      run: |
        python dragons_and_princesses.py input.yaml

  docker:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Build Docker image
      run: |
        docker build -t dragons-princesses:latest .
    
    - name: Test Docker image
      run: |
        docker run --rm dragons-princesses:latest python test_dragons_and_princesses.py
        docker run --rm dragons-princesses:latest python dragons_and_princesses.py input.yaml
